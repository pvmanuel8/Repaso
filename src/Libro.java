/*Crea una clase Libro con los siguientes atributos:

titulo (String)
autor (String)
numeroPaginas (int)
Y métodos:

Constructor para inicializar los atributos.
Método mostrarInformacion() que imprime todos los datos del libro.
Método esLargo() que devuelve true si tiene más de 300 páginas y false en caso contrario.
Tarea:

Crea un par de libros y determina si son largos o cortos. */
public class Libro {

}
